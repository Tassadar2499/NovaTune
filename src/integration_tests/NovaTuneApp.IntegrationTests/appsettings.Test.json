{
  "Kafka": {
    "BootstrapServers": "{{REDPANDA_HOST}}:{{REDPANDA_PORT}}",
    "TopicPrefix": "test",
    "SaslEnabled": false
  },
  "ConnectionStrings": {
    "cache": "{{GARNET_HOST}}:{{GARNET_PORT}}"
  },
  "Cache": {
    "PresignedUrlTtlMinutes": 1,
    "SessionTtlMinutes": 5,
    "KeyPrefix": "test:"
  }
}
